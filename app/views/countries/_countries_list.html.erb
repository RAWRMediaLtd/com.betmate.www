<ul>
	<% countries.each do |country| %>
		<li>
			<%= country.name %> ( <%= country.code %>) - <img src="<%= country.flag %>" alt="<%= country.name %> flag" width="20" height="15">
		</li>
	<% end %>
</ul>
